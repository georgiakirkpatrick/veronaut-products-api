{
  "name": "veronaut-products-api",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "export NODE_ENV=test && mocha --require test/setup.js",
    "dev": "nodemon src/server.js",
    "migrate": "postgrator --config postgrator-config.js",
    "migrate:test": "env NODE_ENV=test && npm run migrate",
    "migrate:production": "heroku run npm run migrate",
    "start": "node src/server.js",
    "predeploy": "npm audit --production",
    "deploy": "git push heroku master",
    "postdeploy": "npm run migrate:production",
    "seed": "psql -U Georgia -d veronaut_products -f './seed.sh'",
    "seed:production": "heroku run psql \\$DATABASE_URL -f \\'./seed.sh\\'"
  },
  "keywords": [],
  "engines": {
    "node": "16.17.0"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "countries-list": "^2.6.1",
    "dotenv": "^16.0.2",
    "express": "^4.17.2",
    "helmet": "^6.0.0",
    "heroku-cli-util": "^8.0.12",
    "jsonwebtoken": "^8.5.1",
    "keypair": "^1.0.4",
    "knex": "^2.3.0",
    "morgan": "^1.10.0",
    "pg": "^8.8.0",
    "postgrator-cli": "^6.0.0",
    "xss": "^1.0.10"
  },
  "devDependencies": {
    "chai": "^4.3.6",
    "heroku": "^7.3.0",
    "mocha": "^10.0.0",
    "nodemon": "^2.0.15",
    "supertest": "^6.2.4"
  }
}
